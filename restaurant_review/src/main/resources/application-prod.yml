environment: production

logging:
  level:
    root: ERROR

spring:
  datasource:
    url: jdbc:h2:mem:se452RRprod
  jpa:
    generate-ddl: true
    show-sql: true
    defer-datasource-initialization: true